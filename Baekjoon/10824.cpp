#include <iostream>
#include <cmath>
#include <cstring>
using namespace std;

int main() {
  string a, b, c, d;
  cin >> a >> b >> c >> d;
cout << (long long)(stoi(a) * pow(10, b.length()) + stoi(b) + stoi(c) * pow(10, d.length()) + stoi(d)) << '\n';
  return 0;
}